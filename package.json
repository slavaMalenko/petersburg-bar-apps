{
  "private": true,
  "workspaces": {
    "packages": [
      "apps/*",
      "packages/*",
      "bff"
    ]
  },
  "scripts": {
    "start-dev": "lerna run start-dev --parallel --ignore petersburg-bar-bff",
    "start-dev-bff": "lerna run start-dev --scope petersburg-bar-bff",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^8.1.6"
  }
}
